apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-config
  namespace: postgres-namespace
  labels:
    app: my-postgres
data:
  POSTGRES_DB: postgresdb
  POSTGRES_USER: admin
  POSTGRES_PASSWORD: pwd